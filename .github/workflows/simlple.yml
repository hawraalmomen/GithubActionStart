name: Shell Commands

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echom "Hej Verden!"

      - name: multiline script
        run: |
          node -v
          npm -v
